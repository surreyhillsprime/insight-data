name: Manual INSIGHT Property Refresh

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  atomic-property-refresh:
    uses: ./.github/workflows/monthly-property-refresh.yml
    secrets: inherit
